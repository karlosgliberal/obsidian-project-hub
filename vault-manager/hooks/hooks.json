{
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [ -f \"${CLAUDE_PROJECT_DIR}/.claude/rules/vault-sync.md\" ]; then echo \"{\\\"continue\\\": true, \\\"suppressOutput\\\": false, \\\"systemMessage\\\": \\\"IMPORTANTE: El contexto se va a comprimir. Antes de perder detalles de esta sesión, ejecuta vault-sync ahora: actualiza el Contexto activo en proyectos/<proyecto>.md, crea entrada en sesiones/ si hubo trabajo significativo, y captura patrones o decisiones descubiertos. Sigue el flujo de /vault-manager:vault-sync.\\\"}\"; fi'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}

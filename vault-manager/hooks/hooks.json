{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [ -f \"${CLAUDE_PROJECT_DIR}/.claude/rules/vault-sync.md\" ]; then echo \"{\\\"continue\\\": true, \\\"suppressOutput\\\": false, \\\"systemMessage\\\": \\\"Recordatorio: este proyecto está conectado al vault de conocimiento. Si hubo trabajo significativo en esta sesión, sugiere al usuario ejecutar /vault-sync antes de terminar.\\\"}\"; fi'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
